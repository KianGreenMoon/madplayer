# Generated by android/icon — a build input, not a source file.
android/icon.png

# APK output.
android/out/

# Compiled binaries. `go build ./...` drops one per main package in the working
# directory — /main from android/icon, /madplayer from cmd/madplayer,
# /meshcheck from cmd/meshcheck.
/main
/madplayer
/meshcheck

# Assistant context. CLAUDE.md is Claude Code's own working notes for this repo,
# and .claude/ is whatever tooling it keeps beside them. Both are read every
# session and neither is part of the program: they describe how to work on this
# code, not the code itself, and they are edited far too often to belong in the
# history. Kept local on purpose.
CLAUDE.md
.claude/
AGENTS.md
